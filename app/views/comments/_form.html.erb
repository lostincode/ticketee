<strong>New comment</strong>
<%= form_for [@ticket, @comment] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :text %>
    <%= f.text_area :text, :size => "30x5" %>
  </p>
  <%= f.submit :class => 'btn' %>
<% end %>
